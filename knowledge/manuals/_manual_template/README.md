# 手册目录（留示意）

电气手册数据（内存分区 / 时钟 / 电压等）是**内容护城河，不开源**。

接手者在此目录放自己的手册解读，格式为一个目录（目录名 = 芯片名），内含：

- `electrical.json`：电气参数（内存分区 / 时钟域 / 电压阈值 / 时序标准）
- `handbook/`：外设手册解读（每个外设的标准值，如 CPOL/CPHA/波特率/时序）

> 字段详细说明见 `docs/DATA_SPEC.md` §7。骨架 `electrical` 模块按此格式读取，
> 填真实数据即可被消费，无需改代码。
